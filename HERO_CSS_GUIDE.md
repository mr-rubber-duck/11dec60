# دليل التحكم في تنسيقات Hero من CSS

## ✅ تم نقل جميع التنسيقات إلى `globals.css`

الآن يمكنك التحكم في جميع تنسيقات Hero من ملف `app/globals.css` بدلاً من inline styles!

---

## 📝 الـ CSS Classes المتاحة للتعديل:

### 1. **القسم الرئيسي**
```css
.hero-poster-section
```
- التحكم في الحجم الكلي للقسم
- الموضع والعرض

### 2. **الفيديو والخلفية**
```css
.hero-video-container  /* حاوية الفيديو */
.hero-video           /* الفيديو نفسه */
.hero-overlay         /* التدرج اللوني فوق الفيديو */
```

### 3. **الإطار الزخرفي**
```css
.hero-frame
```
- لون الحدود
- سمك الحدود
- الموضع

### 4. **المحتوى**
```css
.hero-content-container  /* الحاوية الرئيسية */
.hero-main-content      /* محتوى النص */
```

### 5. **العناوين والنصوص**
```css
.hero-main-title        /* العنوان الرئيسي */
.hero-title-accent      /* النص المميز (يوم نهض الشعب) */
.hero-subtitle          /* العنوان الفرعي */
.hero-description       /* النص الوصفي */
```

### 6. **الخطوط الزخرفية**
```css
.hero-decorative-line          /* الخط الأساسي */
.hero-decorative-line-top      /* الخط العلوي */
.hero-decorative-line-bottom   /* الخط السفلي */
```

### 7. **الأزرار**
```css
.hero-btn              /* الزر الأساسي */
.hero-btn-primary      /* زر "شاهد المعرض" */
.hero-btn-secondary    /* زر "تتبع المسار الزمني" */
.hero-cta-container    /* حاوية الأزرار */
```

### 8. **مؤشر التمرير**
```css
.hero-scroll-indicator  /* الحاوية */
.hero-scroll-text      /* النص "اكتشف المزيد" */
.hero-scroll-mouse     /* شكل الماوس */
.hero-scroll-wheel     /* العجلة المتحركة */
```

---

## 🎨 أمثلة على التعديلات الشائعة:

### تغيير لون العنوان الرئيسي:
```css
.hero-main-title {
    color: #YOUR_COLOR;
}
```

### تغيير حجم الخط:
```css
.hero-main-title {
    font-size: clamp(3rem, 8vw, 6rem); /* زيادة الحجم */
}
```

### تغيير لون التدرج:
```css
.hero-overlay {
    background: linear-gradient(135deg, 
        rgba(YOUR_R, YOUR_G, YOUR_B, 0.85) 0%, 
        rgba(YOUR_R, YOUR_G, YOUR_B, 0.75) 50%, 
        rgba(YOUR_R, YOUR_G, YOUR_B, 0.9) 100%);
}
```

### تغيير لون الأزرار:
```css
.hero-btn-primary {
    border: 3px solid #YOUR_COLOR;
    color: #YOUR_COLOR;
}

.hero-btn-primary:hover {
    background-color: rgba(YOUR_R, YOUR_G, YOUR_B, 0.1);
}
```

### تغيير لون الإطار:
```css
.hero-frame {
    border: 3px solid rgba(YOUR_R, YOUR_G, YOUR_B, 0.3);
}
```

---

## 📱 التعديلات المتجاوبة:

جميع التعديلات المتجاوبة موجودة في نهاية قسم Hero في `globals.css`:

```css
@media (max-width: 768px) {
    /* تعديلات للتابلت */
}

@media (max-width: 480px) {
    /* تعديلات للموبايل */
}
```

---

## 🚀 كيفية التعديل:

1. افتح ملف `app/globals.css`
2. ابحث عن القسم: `/* Hero Poster Section Styles */`
3. عدّل الـ CSS classes حسب رغبتك
4. احفظ الملف - التغييرات ستظهر فوراً!

---

## 💡 نصائح:

- **الألوان**: استخدم نفس نظام الألوان الموجود (#FFF9EC, #9B9B85, #1C1509)
- **الخطوط**: استخدم `clamp()` للحفاظ على الاستجابة
- **الظلال**: `text-shadow` و `box-shadow` لتحسين الوضوح
- **الانتقالات**: `transition` لجعل التأثيرات سلسة

---

## 📍 موقع الملفات:

- **CSS**: `/app/globals.css` (السطر ~638 وما بعده)
- **Component**: `/app/components/hero.jsx`

الآن لديك تحكم كامل في التنسيقات من CSS! 🎉
